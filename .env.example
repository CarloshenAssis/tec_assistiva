# Copie para .env e ajuste os valores locais.
# Supabase fica para uma fase posterior (decisão explícita) — Fase 0 usa
# PostgreSQL local/Docker "puro".

DJANGO_SECRET_KEY=troque-esta-chave-em-producao
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=*

DATABASE_URL=postgres://ciclartech:ciclartech@localhost:5432/ciclartech
